Aufgabe: Fußballteam-Verwaltung

Erstelle eine kleine Java-Anwendung, die ein Fußballteam verwaltet. Dabei sollen Encapsulation, abstrakte Klassen und OOP-Prinzipien angewendet werden.
Anforderungen:

    Abstrakte Klasse Person
        Attribute: name, alter (privat, mit Getter & Setter)
        Abstrakte Methode getRole(), die die Rolle der Person zurückgibt.

    Klasse Spieler (erbt von Person)
        Zusätzliches Attribut: position (privat, mit Getter & Setter)
        Implementiert getRole(), gibt "Spieler" zurück.

    Klasse Trainer (erbt von Person)
        Zusätzliches Attribut: lizenzNummer (privat, mit Getter & Setter)
        Implementiert getRole(), gibt "Trainer" zurück.

    Klasse Team
        Liste von Spielern und ein Trainer (Verwendung von ArrayList)
        Methoden zum Hinzufügen von Spielern/Trainern
        Methode zeigeTeam(), die alle Teammitglieder ausgibt.

Bonus (optional):

    Erstelle eine Methode trainiereTeam(), die eine Trainingsmeldung für alle Spieler ausgibt.

Testen:

    Erstelle im main() ein Team, füge Spieler und einen Trainer hinzu und lasse zeigeTeam() ausführen.

Ziel:

    Du übst Encapsulation, abstrakte Klassen, Vererbung und OOP-Konzepte in einer realistischen Anwendung. 🚀